import { PagePlaceholder } from '@/components/PagePlaceholder';

export function BillingPage() {
  return (
    <PagePlaceholder
      title="Billing"
      prdRef="parent-portal-prd.md §4.8"
      description="Receipts, invoices, refund history (Airwallex)."
    />
  );
}
