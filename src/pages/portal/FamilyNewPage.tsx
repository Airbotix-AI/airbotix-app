import { PagePlaceholder } from '@/components/PagePlaceholder';

export function FamilyNewPage() {
  return (
    <PagePlaceholder
      title="Add a kid"
      prdRef="parent-portal-prd.md §4.2"
      description="Same form as registration step 4; uses existing family consent."
    />
  );
}
