import { PagePlaceholder } from '@/components/PagePlaceholder';

export function ApprovalsPage() {
  return (
    <PagePlaceholder
      title="Approvals"
      prdRef="parent-portal-prd.md §4.5"
      description="Pending kid requests (extra Stars, public share, topic limit change) + recently resolved."
    />
  );
}
