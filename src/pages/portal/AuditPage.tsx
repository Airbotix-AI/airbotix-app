import { PagePlaceholder } from '@/components/PagePlaceholder';

export function AuditPage() {
  return (
    <PagePlaceholder
      title="Activity"
      prdRef="parent-portal-prd.md §4.6"
      description="Family-wide audit feed; filter by kid / type / date; live-tail via WS family:* room."
    />
  );
}
