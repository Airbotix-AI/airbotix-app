import { PagePlaceholder } from '@/components/PagePlaceholder';

export function FamilyListPage() {
  return (
    <PagePlaceholder
      title="My Family"
      prdRef="parent-portal-prd.md §4.2"
      description="Empty / single / multi-kid states; per-kid card with status + Stars used; family-wide controls."
    />
  );
}
