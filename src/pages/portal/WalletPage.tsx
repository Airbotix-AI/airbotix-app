import { PagePlaceholder } from '@/components/PagePlaceholder';

export function WalletPage() {
  return (
    <PagePlaceholder
      title="Wallet"
      prdRef="parent-portal-prd.md §4.4"
      description="Balance + daily/weekly/monthly usage bars + transactions; topup link; pause family."
    />
  );
}
