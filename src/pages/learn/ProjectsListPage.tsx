import { PagePlaceholder } from '@/components/PagePlaceholder';

export function ProjectsListPage() {
  return (
    <PagePlaceholder
      title="My projects"
      prdRef="airbotix-app-learn-prd.md §5"
      description="In-progress + finished projects; create new; class-shared sees teacher review status."
    />
  );
}
