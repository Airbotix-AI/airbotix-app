import { PagePlaceholder } from '@/components/PagePlaceholder';

export function ProjectNewPage() {
  return (
    <PagePlaceholder
      title="New project"
      prdRef="airbotix-app-learn-prd.md §5"
      description="Pick a product line (creative / coding) + mission template or blank slate."
    />
  );
}
