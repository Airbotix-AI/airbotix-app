import { PagePlaceholder } from '@/components/PagePlaceholder';

export function MissionsListPage() {
  return (
    <PagePlaceholder
      title="Missions"
      prdRef="airbotix-app-learn-prd.md §6"
      description="Mission catalogue per enrolled course pack; locked / unlocked / done state per kid."
    />
  );
}
