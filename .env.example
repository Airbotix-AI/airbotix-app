# Backend API base URL (kidsinai/platform-backend)
VITE_API_BASE_URL=http://localhost:8787

# DeepRouter (LLM gateway) is called from the BACKEND, not the frontend.
# This frontend talks only to platform-backend for safety + Stars accounting.
