node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local

# editor / OS
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# logs
*.log
logs

# tests
coverage

# build artifacts (tsc -b composite project leftovers)
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# build-time public env (kept local; CI injects via workflow env)
.env.production

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Vendored Phaser — materialized from the `phaser` npm dep at build/dev time by
# scripts/copy-phaser.mjs (predev/prebuild), served from our own origin (no CDN).
# Tracked via the package.json version pin, not as committed binaries.
/public/vendor/phaser-*.min.js
/public/vendor/phaser-*.d.ts
